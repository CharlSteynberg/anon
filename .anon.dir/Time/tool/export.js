extend(Anon.Time.tool)
({
   export_data:
   {
      icon:'database1',
      keys:'Control x',
      exec:function(inst,evnt)
      {
         dump('make it export like awesomeness');
      },
   },
});
